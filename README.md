# CLI Typing Test by Shivanshu (while watching GOT)

*disclaimer*: this might be incomplete, but i got fun building this, and also i only test on 10 words.

## How to use

first and last step bozo:
`go run *.go`

its that simple.

## Flags:

```
  -length int
        Enter the no. of words (default 10)
  -stop-on-error
        Set wheather to stop on error or not.
```

I use Neovim btw.
